[project]
name = "fsync"
version = "0.2.0"
description = "Simple file indexing and comparison utilities"
authors = [ { name = "dev", email = "dev@example.com" } ]
readme = "readme.md"
dependencies = ["pyyaml", "textual"]

[project.scripts]
fsync = "fsync.cli:main"
[project.optional-dependencies]
progress = ["tqdm"]

[tool.setuptools]
packages = ["fsync"]
